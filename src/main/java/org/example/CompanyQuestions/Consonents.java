package org.example.CompanyQuestions;

//Asked in step2gen company test.
/*
import java.util.*;
// question = Print frequency of each consonant from a line input by a user
//example - "intelligent" - i have to print consonat from this word.
class HelloWorld {

    //creating a method for finding consonants from the word or we can say sentence
    public static void consonantsprinting(String word){
        //lowercasing the word if there is any words in uppercase is there
        word.toLowerCase();
        //declaring map here.
        //for Characters in the string we are declaring the character at first and for there frequency im declaring Integer here for mapping.
        Map<Character, Integer> map = new HashMap<>();
        //running a for loop in the given string
        for(int i=0;i<word.length();i++){

            char ch = word.charAt(i);
            //checking if the character in the string is consonant  here
            //in isvowel im giving a character of string here.
            if(ch >= 'a' && ch <='z' && !isvowel(ch)){
                //printing the consonants
               System.out.print(ch + " ");
               //now i have to print the frequency of the consonants from the string.
        //so for printing the frequency im going to use the map here for there frequency.
            }
        }


    }



    //for vowel here im creating a method when we running a loop and if conditions exist then in condition check isvowel checked.
    //here im returning a vowel that this is vowel words from the string
    public static boolean isvowel(char ch){
        return (ch == 'a' || ch == 'e' || ch == 'i' || ch == 'o' || ch == 'u');
    }

    public static void main(String[] args) {
        //input from the user so im using scanner class here
        Scanner sc = new Scanner(System.in);
        //nextLine used for taking input in String in java
        String word = sc.nextLine();

        //calling a method for printing consonants
        consonantsprinting(word);
        sc.close();
    }
}
 */
public class Consonents {
    public static void main(String[] args) {

    }
}
